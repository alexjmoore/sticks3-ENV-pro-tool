[project]
name = "sticks3-sensor-tool"
version = "0.1.0"
description = "Tooling and MicroPython app for M5Stack StickS3 with ENV Pro sensor"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mpremote>=1.22.0",
    "pillow>=12.3.0",
    "pyserial>=3.5",
]
